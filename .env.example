# GitHub Configuration (Required for data persistence)
GITHUB_REPOSITORY=your_username/meal-planner
GITHUB_TOKEN=your_personal_access_token

# Vercel Configuration (Optional, for local dev mirroring production behavior)
# Set to '1' to enable Vercel-specific logic (read-only filesystem checks etc.)
VERCEL=0

# Server Configuration
PORT=5328

# Frontend Configuration
# NEXT_PUBLIC_... variables if any
