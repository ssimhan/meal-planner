# GitHub API Token (optional for local dev)
# Required for: committing changes back to GitHub from local API
# Get token from: https://github.com/settings/tokens
# Scopes needed: repo (full control)
GITHUB_TOKEN=your_personal_access_token_here

# Repository (auto-detected, override if needed)
# GITHUB_REPOSITORY=ssimhan/meal-planner
