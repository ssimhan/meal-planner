# /5-latest

You are displaying the most recent meal plan.

Goal:
- Show the user the most recently created meal plan
- No date needed - automatically find the latest

Actions:
1. Run: `./mealplan 5-latest`
2. Alternatively, find the most recent file in `plans/` directory and read it
3. Present the plan to the user in a readable format

Notes:
- This is useful for quick reference to the current week's plan
- The command automatically finds the newest plan file by date

Input:
<<USER_REQUEST>>
