# /1-start

You are helping start a new week for the meal planning system.

Goal:
- Run the `./mealplan 1-start` command to create a new weekly input file
- This will launch an interactive prompt for the user to fill in schedule details
- The command will create a new file in `inputs/YYYY-MM-DD.yml`

Actions:
1. Run: `./mealplan 1-start`
2. Wait for the interactive prompts to complete
3. Confirm the new input file was created successfully
4. Optionally, read the created file to verify its contents

Input:
<<USER_REQUEST>>
